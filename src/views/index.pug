html
    head
        title Index

    body(class=['myClass'])
        h1(id='myId')= title
        p
            h3 sub Header
            ul
                each val in list
                    li= val